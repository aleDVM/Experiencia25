<div class="container">
  <h1>El registro fue almacenado con éxito!</h1>
  <div class="table-responsive">
    <table class="table">
      <tr>
        <th>Codigo</th>
        <th>Detalle</th>
        <th>Categoria</th>
        <th>Valor</th>
        <th>Descuento</th>
        <th>Total</th>
      </tr>
        <%@sales.each do |sale|%>
      <tr>
        <td><%= sale.cod%></td>
        <td><%= sale.detail%></td>
        <td><%= sale.category%></td>
        <td><%= sale.value%></td>
        <td><%= sale.discount%></td>
        <td><%= sale.total%></td>
      </tr>  <%end%>
    </table>
  </div>
  <%= link_to 'Volver', sales_new_path%>
</div>
